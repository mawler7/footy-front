import React from 'react';
import { Spinner, LoadingWrapper } from '../../styles/content/GlobalStyles';

const LoadingSpinner = () => (
    <LoadingWrapper>
        <Spinner />
    </LoadingWrapper>
);

export default LoadingSpinner;
